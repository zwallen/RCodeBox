% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gex_counts.R
\docType{data}
\name{gex_counts}
\alias{gex_counts}
\title{TCGA COAD/READ gene expression (STAR counts)}
\format{
A numeric matrix-like object (e.g., \code{matrix} or \code{dgCMatrix})
with genes in rows and samples in columns:
\itemize{
\item \strong{rownames}: Ensembl gene IDs matching \code{\link{gex_data}}.
\item \strong{colnames}: TCGA sample barcodes (e.g., \code{TCGA-XX-YYYY-01A}).
\item \strong{values}: Integer counts (library-size dependent).
}
}
\source{
National Cancer Institute Genomic Data Commons (GDC), TCGA-COAD and
TCGA-READ; retrieved via \pkg{TCGAbiolinks}.
}
\usage{
gex_counts
}
\description{
Raw (or harmonized) gene-level counts produced by STAR-based RNA-seq
quantification for TCGA \strong{COAD} and \strong{READ} samples.
}
\details{
Columns correspond to samples of type \code{"Primary Tumor"} and
\code{"Solid Tissue Normal"} from projects \code{"TCGA-COAD"} and
\code{"TCGA-READ"} as specified in the query used to retrieve the data.
Consider using functions such as \code{edgeR::calcNormFactors} or
\code{DESeq2::DESeq} for downstream normalization and differential analysis.
}
\examples{
data(gex_counts)
dim(gex_counts)
gex_counts[1:5, 1:5]
}
\seealso{
\code{\link{gex_tpm}}, \code{\link{gex_data}}, \code{\link{clinical_data}}
}
\keyword{datasets}
