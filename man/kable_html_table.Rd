% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kable_html_table.R
\name{kable_html_table}
\alias{kable_html_table}
\title{Create a styled HTML table with kableExtra}
\usage{
kable_html_table(df, bold_columns = 1, caption = NULL)
}
\arguments{
\item{df}{Input table data. The first column is used to detect section headers for
border styling via a regex match on \code{"N (\%)"} or \code{"Mean±SD"}.}

\item{bold_columns}{Column indices to render in bold (e.g., \code{1} or \code{1:2}). Use \code{NULL} or
\code{integer(0)} to skip bolding. (default: \code{1})}

\item{caption}{Caption to give the table.}
}
\value{
A \code{kableExtra} HTML table object (class typically including \code{knitr_kable})
that can be printed in HTML contexts (R Markdown/Quarto, Shiny).
}
\description{
Renders a centered, scroll-friendly HTML table from a data frame using
\code{kableExtra} (via \code{kbl()}), with a fixed header, bolded leading columns,
compact header styling, and sectioning borders for summary rows that contain
\code{"N (\%)"} or \code{"Mean±SD"} in the first column. Columns are given a minimum CSS
width based on the maximum string length observed in each column, and all
body text is set to a small, consistent font size for compact display.
}
