% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/us_map_scatterpie.R
\name{us_map_scatterpie}
\alias{us_map_scatterpie}
\title{Create a US map with scatter pie charts by state}
\usage{
us_map_scatterpie(
  df,
  column,
  state_column,
  count_column,
  legendlab = NULL,
  color_list = NULL,
  pie_scale = 1.75,
  pie_alpha = 0.6,
  save = FALSE,
  figwidth = 3000,
  figheight = 3000,
  out_path = "us_map_scatterpie.jpg"
)
}
\arguments{
\item{df}{Dataframe containing the categorical variable, state column, and count column.}

\item{column}{Name of the categorical variable in \code{df} to visualize in the pie charts.}

\item{state_column}{Name of the column in \code{df} containing US state abbreviations (e.g., "CA", "NY").}

\item{count_column}{Name of the column in \code{df} to use for counting. Each unique value should
represent one observation/case.}

\item{legendlab}{Title for the legend. (default is to use name given to \code{column})}

\item{color_list}{Vector of R recognized color strings the length of the number of categories in
the variable provided to \code{column}. If NULL, uses RColorBrewer Set3 palette.}

\item{pie_scale}{Scaling factor for the pie chart sizes. Larger values create bigger pies.
(default: 1.75)}

\item{pie_alpha}{Transparency level for the pie charts, from 0 (fully transparent) to 1
(fully opaque). (default: 0.6)}

\item{save}{Whether to save the image to file. (default: FALSE)}

\item{figwidth}{Width of the output image file in pixels (\code{px}).}

\item{figheight}{Height of the output image file in pixels (\code{px}).}

\item{out_path}{File path for the output image file. Existing files at this path are overwritten.
Whatever extension included in the image file name (e.g., jpg, png, pdf) will
be the format that is outputted.}
}
\value{
A \code{ggplot2} figure object. If save is \code{TRUE}, also exports the image to file.
}
\description{
This function creates a US map with scatter pie charts positioned at state
centroids, showing the composition of a categorical variable for each state.
Alaska and Hawaii are repositioned for better visualization.
}
