% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pie_chart.R
\name{pie_chart}
\alias{pie_chart}
\title{Create a pie chart with automatic labeling}
\usage{
pie_chart(
  df,
  column,
  count_column,
  repel_label_threshold = 0.05,
  legendlab = NULL,
  color_list = NULL,
  save = FALSE,
  figwidth = 1000,
  figheight = 1000,
  out_path = "pie_chart.jpg"
)
}
\arguments{
\item{df}{Dataframe containing the categorical variable and count column.}

\item{column}{Name of the categorical variable in \code{df} to create the pie chart for.}

\item{count_column}{Name of the column in \code{df} to use for counting. Each unique value should
represent one observation/case.}

\item{repel_label_threshold}{Frequency threshold for pushing labels outside of the pie chart. Any slices
of the pie chart less than this will have its labels repelled outside of the
pie chart area.}

\item{legendlab}{Title for the legend. (default is to use name given to \code{column})}

\item{color_list}{Vector of R recognized color strings the length of the number of categories in
the variable provided to \code{column}. If NULL, uses RColorBrewer Set3 palette.}

\item{save}{Whether to save the image to file. (default: FALSE)}

\item{figwidth}{Width of the output image file in pixels (\code{px}).}

\item{figheight}{Height of the output image file in pixels (\code{px}).}

\item{out_path}{File path for the output image file. Existing files at this path are overwritten.
Whatever extension included in the image file name (e.g., jpg, png, pdf) will
be the format that is outputted.}
}
\value{
A \code{ggplot2} figure object. If save is \code{TRUE}, also exports the image to file.
}
\description{
This function creates a pie chart for a categorical variable, automatically
calculating counts and percentages. Labels are placed directly on slices that
represent at least 5\% of the total, while smaller slices are labeled with
repelled labels to avoid overlap.
}
