% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stratified_stat_table.R
\name{stratified_stat_table}
\alias{stratified_stat_table}
\title{Create a stratified summary table with regression-based comparisons}
\usage{
stratified_stat_table(
  df,
  groups,
  columns,
  rename_dict = NULL,
  covariates = NULL,
  omics_mode = FALSE
)
}
\arguments{
\item{df}{Input dataframe containing the grouping variable, columns to summarize,
and any optional covariates.}

\item{groups}{Name of the grouping variable in \code{df}. Each unique value defines a group
for stratified summaries and serves as the binary indicator (vs. all others)
in the regression models.}

\item{columns}{Names of columns in \code{df} to summarize and test.}

\item{rename_dict}{Named list with optional mapping from original column names to display labels.
If provided, \code{rename_dict[col]} is used in the "Variable" column of the output;
otherwise, the original name is used. (Function expects that all \code{columns}
exist as names in \code{rename_dict} when provided.)}

\item{covariates}{Optional names of additional columns in \code{df} to include as covariates
in the regression models for between-group comparisons.}

\item{omics_mode}{Whether to modify the workflow of this function to process omics data or other
big data types created via a high-throughput method. Modifications include:
\itemize{
\item Skipping of formatting variable names
\item Not adding extra verbage to the variable names
\item Performing multiple testing correction via false discovery rate method
\item Separating the resulting coefficients and confidence intervals for easier
post-analysis plotting of results
(default: FALSE)
}}
}
\value{
A \code{data.frame} where rows correspond to categories (for categorical variables)
or a single row (for numeric variables) per input variable, and columns include
the variable label, categories, total summary, stratified group summaries, and
regression results (Coefficient and P-value).
}
\description{
Builds a publication-style table of summary statistics for selected columns,
including totals and group-stratified estimates, and performs regression-based
tests of difference between groups. Categorical variables are summarized as
counts and percentages and compared using Firth's penalized logistic
regression (one-vs-category with a group indicator); numeric variables are
summarized as mean \eqn{\pm}{+/-} SD and compared using linear regression. Optional
covariates can be included to adjust the models.
}
