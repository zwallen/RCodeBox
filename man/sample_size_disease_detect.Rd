% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_size_disease_detect.R
\name{sample_size_disease_detect}
\alias{sample_size_disease_detect}
\title{Calculate sample size for clinical studies involving assays for
disease detection}
\usage{
sample_size_disease_detect(
  disease_prevalence,
  dropout_rate,
  failure_rate = 0,
  sensitivity = 0.9,
  specificity = 0.85,
  precision = 0.1,
  confidence_interval = 0.95,
  target_power = 0.9,
  n_sim = 1000,
  step_size = 10,
  max_iter = 10000,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{disease_prevalence}{Expected disease prevalence (prevalence among enrolled patients).}

\item{dropout_rate}{Expected dropout rate (proportion of enrolled patients who do not complete
follow-up).}

\item{failure_rate}{Expected assay failure rate (proportion of successfully followed samples that
fail testing) (default: 0).}

\item{sensitivity}{Assay sensitivity (default: 0.9 or 90\%).}

\item{specificity}{Assay specificity (default: 0.85 or 85\%).}

\item{precision}{Half-width of desired confidence interval for sensitivity/specificity
(default: 0.1 or \eqn{\pm 10\%}).}

\item{confidence_interval}{Confidence level for sensitivity/specificity estimation (default: 0.95 or 95\%).}

\item{target_power}{Desired probability that the simulated confidence interval half-widths for
sensitivity and specificity will be less than or equal to \code{precision}
(default: 0.9).}

\item{n_sim}{Number of Monte Carlo simulation replicates per candidate sample size
(default: 1000).}

\item{step_size}{Increment to increase the total sample size when iteratively searching for
a sample size that meets \code{target_power} (default: 10).}

\item{max_iter}{Maximum number of iterations to attempt when searching for a convergent
sample size (default: 10000).}

\item{seed}{Random seed for reproducible simulation results (default: 42).}

\item{verbose}{Logical; if TRUE progress messages are emitted during the iterative search
(default: TRUE).}
}
\value{
A named list with the following elements:
\itemize{
\item final_sample_size: integer, the total number of patients required (after
adjustment for dropout and assay failure).
\item power_sensitivity: numeric, estimated probability that the sensitivity
confidence interval half-width is <= \code{precision} under the simulated design.
\item power_specificity: numeric, estimated probability that the specificity
confidence interval half-width is <= \code{precision} under the simulated design.
\item required_diseased: integer, expected number of diseased (relapse) patients
within the final sample.
\item required_non_diseased: integer, expected number of non-diseased patients
within the final sample.
\item iterations: integer, number of iterations performed in the search loop.
\item statement: character, human-readable summary of the final sample size and
assumptions used.
}
}
\description{
Calculates the total sample size needed for a clinical studies aimed at assessing
the performance of a disease detection assay. The function uses Buderer's
formulas (Buderer, 1996) to obtain an initial analytic estimate for sensitivity
and specificity then refines that estimate via Monte Carlo simulation to
achieve a target probability (power) that the resulting confidence interval
half-widths for sensitivity and specificity do not exceed the requested
precision. The calculation accounts for expected disease prevalence,
participant dropout, and assay failure.
}
\references{
Buderer (1996). Statistical methodology: I. Incorporating the prevalence of
disease into the sample size calculation for sensitivity and specificity.
Academic Emergency Medicine, 3(9), 895-900.
}
